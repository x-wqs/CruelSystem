chubby和spanner是基于paxos的但是没有公开发表。
目前没有公开发表的代码是基于paxos的。

VR算法跟Raft比较像，都是基于领导人的算法。

raft跟其他算法的异同 ：
1 raft强领导性，paxos强领导人不是必须的
2 raft比VR和zookeeper拥有更少机制，日志只从领导人流出，而不是双向的
3 raft跟VR和zookeeper相比信息类型更少，更简单。（实践中应该有更多信息）

总结：可理解性在一致性算法中也很重要
