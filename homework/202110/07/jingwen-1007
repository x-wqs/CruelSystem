###一致性哈希解决平分load 的问题

#### 当需要增加一个服务器的时候：

只需要把增加前最后一个服务器的度数 到 增加的服务器的度数 之间的哈希值migrate到新的服务器上面就可以了

问题：

新服务器增加在那呢？无论增加在哪都不均匀了不是吗

下一个新增服务器又在哪个度数上呢

#### 当需要减少一个服务器的时候：

只需要把删除前最后一个服务器的度数 到 删除后顺势针的下一个服务器 之前的哈希值migrate到 删除后顺势针的下一个服务器 就可以了

实际上可以用虚拟node，而不需要真正建立一个物理的环



